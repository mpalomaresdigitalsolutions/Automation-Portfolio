name = "portfolio-chat-proxy"
main = "workers/chat-proxy.js"
compatibility_date = "2025-04-01"
account_id = "7727bfc4627263a4d0776348c0ba3f96"

[vars]
ALLOWED_ORIGIN = "https://mpalomaresdigitalsolutions.site"
RATE_LIMIT = "20"

# ====================================================
# Required environment variables — set via Cloudflare Dashboard
# ====================================================
# DS_KEY           — DeepSeek API key (required)
# ALLOWED_ORIGIN   — Your portfolio domain, e.g. https://mpalomaresdigitalsolutions.site
# RATE_LIMIT       — Max requests per IP per minute (default: 20)
# TURNSTILE_SECRET — Cloudflare Turnstile secret key (optional, for CAPTCHA)
